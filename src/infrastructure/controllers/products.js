const getProducts = (req, res, next) => {
  res.status(200).send()
  return next()
}

const getProduct = (req, res, next) => {
  res.status(200).send()
  return next()
}

module.exports = { getProducts, getProduct }
